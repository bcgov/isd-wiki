apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "isdWikiServer.fullname" . }}-test-connection"
  labels:
    {{- include "isdWikiServer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "isdWikiServer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
