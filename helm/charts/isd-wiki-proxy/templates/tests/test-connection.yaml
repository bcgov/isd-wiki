apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mediawikiServer.fullname" . }}-test-connection"
  labels:
    {{- include "mediawikiServer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mediawikiServer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
