apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "isdWiki.fullname" . }}-html
  labels:
    {{- include "isdWiki.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi