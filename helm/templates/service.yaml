apiVersion: v1
kind: Service
metadata:
  name: {{ include "isdWiki.fullname" . }}
  labels:
    {{- include "isdWiki.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "isdWiki.selectorLabels" . | nindent 4 }}
